---
editor_options: 
  markdown: 
    wrap: 72
---

```{r}
library(dplyr)
library(tidyr)
library(knitr)
library(kableExtra)
options(kableExtra.latex.load_packages = TRUE)
library(ggplot2)
library(scales)
library(corrplot)
library(GGally)
library(caret)
library(tidymodels)
library(themis)
library(boot)
library(car)
library(QuantPsyc)
library(conflicted)
library(pROC)
library(caret)
conflict_prefer("select", "dplyr")
conflict_prefer("filter", "dplyr")
conflict_prefer("step","stats")
```

# Tyrimas

## Duomenų aprašymas

Duomenys atsisiųsti iš *UCI Irvine Machine Learning Repository*
[@online_shoppers_purchasing_intention_dataset_468] su iš viso 10535
duomenų eilučių. Duomenų kintamieji aprašyti \ref{tab:num_features} ir
\ref{tab:cat_features} lentelėse. Duomenų rinkinį sudaro 1 metų
informacija, kuri yra surinkta iš *columbia.com.tr* puslapio
[@columbia_website].

```{r}
num_features <- data.frame(
  "Kintamojo pavadinimas" = c(
    "Administraciniai puslapiai", 
    "Administracinių puslapių trukmė", 
    "Informaciniai puslapiai", 
    "Informacinių puslapių trukmė", 
    "Produktų puslapių trukmė", 
    "Atšokimo dažnis", 
    "Išėjimo dažnis", 
    "Puslapio vertė", 
    "Ypatinga diena"
  ),
  "Kintamojo aprašymas" = c(
    "Kiek kartų lankytojas lankėsi paskyros valdymo puslapiuose.",
    "Bendras laikas (sekundėmis), kurį lankytojas praleido paskyros valdymo puslapiuose.",
    "Kiek kartų lankytojas lankėsi informaciniuose puslapiuose.",
    "Bendras laikas (sekundėmis), kurį lankytojas praleido informaciniuose puslapiuose.",
    "Bendras laikas (sekundėmis), kurį lankytojas praleido parduodamų daiktų puslapiuose.",
    "Vidutinis lankytojo aplankytų puslapių skaičius, prieš pabaigiant sesiją, kai aplankyta tik viena prekė.",
    "Vidutinis lankytojo aplankytų puslapių skaičius, prieš pabaigiant sesiją, kai aplankyta daugiau nei viena prekė.",
    "Vidutinė lankytojo aplankytų puslapių vertė.",
    "Svetainės apsilankymo laiko artumas ypatingai dienai (pvz., šventei, išpardavimui)."
  ),
  check.names = FALSE
)

knitr::kable(num_features, 
      caption = "Kiekybinių kintamųjų aprašymas\\label{tab:num_features}")
```

```{r}
cat_features <- data.frame(
  "Kintamojo pavadinimas" = c(
    "Operacinė sistema", 
    "Naršyklė", 
    "Regionas", 
    "Srauto tipas", 
    "Lankytojo tipas", 
    "Savaitgalis", 
    "Mėnuo", 
    "Pelningumas"
  ),
  "Kintamojo aprašymas" = c(
    "Lankytojo operacinė sistema.",
    "Lankytojo interneto naršyklė.",
    "Geografinis regionas, iš kurio lankytojas pradėjo sesiją.",
    "Šaltinis, iš kurio lankytojas atvyko į svetainę (pvz., reklama kitame puslapyje, SMS, tiesioginis).",
    "Lankytojo tipas: \"Naujas lankytojas\", \"Sugrįžtantis lankytojas\" ir \"Kitas\"",
    "Ar apsilankymo data sutampa su savaitgaliu.",
    "Apsilankymo datos mėnuo.",
    "Ar sesija buvo pelninga."
  ),
  check.names = FALSE
)

knitr::kable(cat_features, 
      caption = "Kategorinių kintamųjų aprašymas\\label{tab:cat_features}")
```

## Duomenų paruošimas

Kintamieji „Operacinė sistema“, „Naršyklė“, „Regionas“ ir „Srauto tipas“
yra kategoriniai kintamieji, užkoduoti natūraliais skaičiais. Todėl juos
pakeičiame atitinkamais faktoriais ir vėliau iš jų formuosime fiktyvius
kintamuosius. Kintamasis „SpecialDay“, sprendžiant pagal reikšmes ir
aprašymą yra ranginis kitamasis įgaunantis 6 skirtingas reikšmes:
$\{0;\ 0,2;\ \dots;\ 1\}$.

```{r}
# duomenu atsisiuntimo nuoroda: https://archive.ics.uci.edu/dataset/468/online+shoppers+purchasing+intention+dataset

df <- read.csv(
  "data//online_shoppers_intention.csv",
  header=TRUE,
  stringsAsFactors=FALSE
)


int_faktoriai <- c(
  "OperatingSystems",
  "Browser",
  "Region",
  "TrafficType"
)
df[int_faktoriai] <- lapply( df[int_faktoriai], as.factor )

df <- df %>% 
  select( -c("ProductRelated") )
```

Duomenų aprašyme autoriai teigia, kad praleistų reikšmių nėra. Mes
pasitikslinome, pasileidę atitinkamą *R* kodą - praleistų reikšmių nėra.

```{r, echo=FALSE, include=FALSE}
missing_table <- data.frame(
  Praleista = colSums(is.na(df))
)

missing_table
```

Loginių išskirčių tyrimas yra būtina dalis tyrimo, kadangi kai kurios iš
duomenų rinkinyje esančios sesijos gali būti informacijos surinkimo
robotai (*angl. web-scrapers*). Kad ištirtume logines išskirtis sudarėme
skaitinių charakteristikų lentelę kiekybiniams kintamiesiems
(\ref{tab:char} lentelė). Joje naudojami standartiniai skaitinių
charakteristikų žymėjimai: Min - minimumas, $Q_n$ - $n$-tasis kvartilis,
$\mu$ - vidurkis, Max - maximumas. \ref{tab:char} lentelėje matome, jog
nėra jokių nelogiškai išsiskiriančių reikšmių kintamuosiuose
nesusijusiuose su apsilankymo trukme. Tačiau pastebėjome, kad
kintamieji, susijusiems su sesijos trukme, turi loginių išskirčių, kurių
tvarkybą pakomentuosim sekančiame paragrafe.

```{r}
summary_tbl <- df %>%
  summarise(
    across(
      where(is.numeric),
      list(
        Min      = ~min(.x, na.rm = TRUE),
        `$Q_1$`  = ~quantile(.x, 0.25, na.rm = TRUE),
        `$Q_2$`  = ~median(.x, na.rm = TRUE),
        `$\\mu$` = ~mean(.x, na.rm = TRUE),
        `$Q_3$`  = ~quantile(.x, 0.75, na.rm = TRUE),
        Max      = ~max(.x, na.rm = TRUE)
      ),
      .names = "{.col}__{.fn}"
    )
  ) %>%
  pivot_longer(
    cols = everything(),
    names_to = c("Kintamasis", "Statistika"),
    names_pattern = "^(.*)__(.*)$"
  ) %>%
  pivot_wider(names_from = Statistika, values_from = value) %>%
  mutate(
    across(where(is.numeric), ~round(.x, 2)),
    Kintamasis = dplyr::recode(Kintamasis,
      "Administrative"          = "Administraciniai puslapiai",
      "Administrative_Duration" = "Laikas administraciniuose puslapiuose",
      "Informational"           = "Informaciniai puslapiai",
      "Informational_Duration"  = "Laikas informaciniuose puslapiuose",
      "ProductRelated"          = "Produktų puslapiai",
      "ProductRelated_Duration" = "Produktų puslapių trukmė",
      "BounceRates"             = "Atšokimo dažnis",
      "ExitRates"               = "Išėjimo dažnis",
      "PageValues"              = "Puslapio vertė",
      "SpecialDay"              = "Ypatinga diena"
    )
  )

knitr::kable(
  summary_tbl,
  digits = 2, format.args = list(decimal.mark = ","),
  caption = "Kiekybinių kintamųjų skaitinės charakteristikos\\label{tab:char}"
)
```

Kad susitvarkytume su išskirtimis, išimsime stebėjimus, kai bendras
praleistas laikas svetainėse yra nulis. Taip pat, pašalinsime
stebėjimus, jei laikas tam tikram svetainių tipui yra nenulinis (pvz.
nurodyta, kad administraciniuose puslapiuose praleistas tam tikras
laikas), tačiau to tipo puslapių nebuvo aplankyta.

```{r}
df <- df %>%
  filter(
    Administrative_Duration + Informational_Duration + ProductRelated_Duration != 0,
    !(Administrative == 0 & Administrative_Duration != 0),
    !(Informational == 0 & Informational_Duration != 0),
    !(Administrative > 0 & Administrative_Duration == 0),
    !(Informational > 0 & Informational_Duration == 0)
  )
```

## Duomenų pradinė analizė

Prieš apmokant regresijos modelius labai svarbu įsitikinti, kad
kategorinio kintamojo lygmenų kartotinumas yra pakankamai didelis, nes
lygmenims, turintiems mažai stebėjimų, negalėsime daryti išvadų. Toliau
pašaliname kategorinių kintamųjų lygmenis, turinčius 5 ar mažiau
stebėjimų.

Iš žemiau pateiktų lentelių, matome, kad yra probleminių faktorių
lygmenų. „Naršyklės" devintas lygmuo, turi mažiau nei 5 stebėjimus. Tuo
tarpu faktorius „Srauto tipas" turi tris probleminius lygmenis, kuriuos
mes išimsime: būtent 12-as, 16-as ir 17-as. Šių faktorių dažnių lentelės
pateiktos atitinkamai \ref{tab:Browser} ir \ref{tab:TrafficType}
lentelėse.

\newpage

```{r}
make_freq_table <- function(data, column, label = column) {
  result <- data %>%
    group_by(!!sym(column)) %>%
    summarise(Dažnis = n(), .groups = "drop") %>%
    mutate(Procentai = round(Dažnis / sum(Dažnis) * 100, 1))
  
  knitr::kable(
    result,
    caption = paste0(label, " dažnių lentelė\\label{tab:", column, "}"),
    col.names = c(label, "Dažnis", "Santykinis dažnis (%)")
  ) %>%
    kableExtra::row_spec(
      which(result$Dažnis < 5),
      bold = TRUE, background = "#FFD700"
    )
}
```

```{r echo=FALSE}
make_freq_table(df, "Browser",          "Naršyklė")
make_freq_table(df, "TrafficType",      "Srauto tipas")
```

```{r}
df <- df %>%
  filter(Browser != 9) %>%
  filter(!TrafficType %in% c(12, 16, 17))
```

Toliau tiriame kiekybinių požymių sąryšius su atsaku - laiku, praleistu
produktų puslapiuose. Sąryšius tiriame, pasitelkę sklaidos diagramų
groteles (žr. \ref{fig:scatterplots} pav.), kurių x ir y ašims
pritaikyta logaritminė transformacija. Pastebime, kad turime tam tikrą
koreliacija su visomis potencialiomis kovariantėmis išskyrus kintamąjį
„Ypatinga diena“. Aplankytų administracinių, informacinių ir produktų
puslapių skaičius natūraliai reiškia didesnę trukmę, praleistą produktų
puslapiuose, taigi pastebime teigiamą koreliaciją. Neigiamą puslapių
kokybę atskleidžiančios „Google Analytics“ metrikos „Atšokimo dažnis ir
Išėjimo dažnis turi neigiamą koreliaciją su. Kas irgi yra akivaizdu -
kuo naudotojo aplankyti puslapiai yra labiau linkę būti sesijos
pabaigimo priežastimi ar būti praversti nesukūrus jokios užklausos
puslapyje, tuo puslapiai yra labiau linkę skatinti naudotoją atsijungti
nuo internetinės parduotuvės. Tuo tarpu puslapio Google Analytics vertė
turi neigiamą koreliaciją su atsaku, kas nėra intuityvu. Tai galimai yra
konkurentų puslapiai, turintys aukštus įvertinimus ar kita problema, į
kurią reikėtų iš dalykinės pasižiūrėti.

Šias išvadas patvirtina ir \ref{tab:spearman_coef} lentelė.

```{r, fig.width=8, fig.height=6, fig.cap="Sklaidos diagramos, tarp kiekybinių kovariančių ir atsako - laiko, kurį naudotojas praleidžia internetinėje parduotuvėje\\label{fig:scatterplots}"}

var_labels <- c(
  "Administrative"          = "Administraciniai puslapiai",
  "Administrative_Duration" = "Laikas administraciniuose\npuslapiuose",
  "Informational"           = "Informaciniai puslapiai",
  "Informational_Duration"  = "Laikas informaciniuose\npuslapiuose",
  "BounceRates"             = "Atšokimo dažnis",
  "ExitRates"               = "Išėjimo dažnis",
  "PageValues"              = "Puslapio vertė",
  "SpecialDay"              = "Ypatinga diena"
)

col_subset <- names(var_labels)

df_long <- df %>%
  filter(
    PageValues != 0,
    BounceRates != 0,
    Administrative_Duration != 0,
    Informational_Duration != 0
  ) %>%
  tidyr::pivot_longer(
    cols = all_of(col_subset),
    names_to = "variable",
    values_to = "value"
  ) %>%
  mutate(variable = var_labels[variable])

ggplot(df_long, aes(x = value, y = ProductRelated_Duration)) +
  geom_point(alpha = 0.45, size = 0.4, color = "#2C3E50") +
  geom_smooth(method = "lm", color = "#E74C3C", linewidth = 0.8, se = TRUE) +
  facet_wrap(~ variable, scales = "free_x", ncol = 3) +
  scale_x_continuous(
    trans  = "log1p",
    breaks = scales::trans_breaks("log1p", function(x) round(exp(x) - 1))
  ) +
  scale_y_continuous(
    trans  = "log1p",
    breaks = c(0, 10, 100, 1000, 10000)
  ) +
  labs(x = NULL, y = "Trukmė produktų puslapiuose") +
  theme_minimal() +
  theme(
    strip.text       = element_text(color = "black", face = "bold", size = 9),
    panel.grid.minor = element_blank(),
    axis.text        = element_text(size = 12)
  )
```

```{r}
cor_table <- sapply(col_subset, function(var) {
  cor.test(df[[var]], df$ProductRelated_Duration, method = "spearman")
}) |>
  t() |>
  as.data.frame() |>
  transmute(
    variable = var_labels[col_subset],
    rho      = round(unlist(estimate), 3)
  )

row.names(cor_table) <- NULL

knitr::kable(
  cor_table,
  col.names = c("Kintamasis", "Spearmano koeficientas"),
  caption   = "Spirmano koreliacijos su trukme apsilankymų produktų puslapiuose\\label{tab:spearman_coef}"
) %>%
  kableExtra::row_spec(which(abs(cor_table$rho) > 0.25), bold = TRUE)
```

Iš stulpelinių grafikų, su 95 % procentilinių savirankos intervalais,
(žr. \ref{fig:barchart} pav.) pastebime, kad:

1.  Lankytojo tipas, mėnuo, kurio metu fiksuota naudotojo sesija,
    naudotojo naršyklė ir naudojama operacinė sistema turi reikšmingai
    skirtingus vidurkius praleisto laiko produktų puslapiuose.
2.  Laikas, praleistas produktų puslapiuose vidutiniškai nepriklauso nuo
    to, ar sesija fiksuota savaitgalį, ar ne.
3.  Tik pavieniai regionai išsiskiria laiko, praleisto produktų
    puslapiuose prasme, tačiau bendrai paėmus labai panašus atsako
    vidurkis visuose lygmenyse.

```{r fig.cap="Stulpeliniai grafikai tarp kokybinių kovariančių ir laiko, praleisto produktų puslapiuose tiriamoje internetinėje parduotuvėje\\label{fig:barchart}"}
var_labels2 <- c(
  "Month"           = "Mėnuo",
  "OperatingSystems" = "Operacinė sistema",
  "Browser"         = "Naršyklė",
  "Region"          = "Regionas",
  "VisitorType"     = "Lankytojo tipas",
  "Weekend"         = "Savaitgalis"
)

col_subset2 <- names(var_labels2)

boot_ci <- function(x, bound, R = 500) {
  b  <- boot::boot(x, function(d, i) mean(d[i]), R = R)
  ci <- boot::boot.ci(b, type = "perc")
  ci$percent[bound]
}

df_long2 <- df %>%
  mutate(across(all_of(col_subset2), as.factor)) %>%
  tidyr::pivot_longer(
    cols = all_of(col_subset2),
    names_to = "variable",
    values_to = "value"
  ) %>%
  mutate(variable = var_labels2[variable])

df_summary <- df_long2 %>%
  group_by(variable, value) %>%
  summarise(
    mean_y = mean(ProductRelated_Duration),
    lower  = boot_ci(ProductRelated_Duration, 4),
    upper  = boot_ci(ProductRelated_Duration, 5),
    .groups = "drop"
  )

ggplot(df_summary, aes(x = value, y = mean_y)) +
  geom_col(fill = "#2C3E50") +
  geom_errorbar(aes(ymin = lower, ymax = upper),
                width = 0.3, color = "#E74C3C") +
  facet_wrap(~ variable, scales = "free_x", ncol = 3) +
  labs(x = NULL, y = "Trukmė produktų puslapiuose") +
  theme_minimal() +
  theme(
    strip.text       = element_text(color = "black", face = "bold", size = 9),
    panel.grid.minor = element_blank(),
    axis.text.x      = element_text(angle = 45, hjust = 1, size = 7)
  )
```
